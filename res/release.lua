RELEASE = {
    ---是否打开测试快捷键
    IS_DEV_KEYBOARD = true,
    ---是否打开调试显示
    IS_BASENODE_DEBUGGRAPH = false,
    ---是否将远端节点cache到本地
    ASYNC_TO_REMOTE = false,
    ---DEBUG IMGUI MENU 
    IMGUI_DEBUG_MENU = false
}
